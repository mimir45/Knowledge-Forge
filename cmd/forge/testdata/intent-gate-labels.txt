# Intent-gate ground truth — the prompts cmd/forge/intent.go's threshold is derived from.
#
# B-033 pairs the neighbour floor with intent.go's hardcoded 0.7 because both were
# calibrated against the pre-B-008 scale. They share a root cause, not an answer: the two
# gates have opposite cost functions. A neighbour false positive is one wrong wikilink in
# a note under review. An intent false positive interrupts a user prompt with injected
# context, on every prompt, in a hook contracted never to disturb the session. So this
# gate is derived for precision where the floor is derived for recall.
#
# FIRE  — the vault genuinely already answers this; surfacing the hit is the point.
#         Each is phrased as a user would type it, against a note that exists.
# QUIET — the vault does not answer this. Adjacent-topic prompts, which are exactly the
#         hard negatives (a related note exists and scores), plus off-corpus prompts,
#         which must never fire.
#
# Written from the corpus slug list before any score was measured, same rule as
# neighbour-labels.txt.

FIRE: why does @Value not work for a YAML list in Spring Boot
FIRE: how does keyset pagination work
FIRE: why is Continue.dev sending zero requests to llama.cpp
FIRE: why does Qwen3 return a blank response in Continue.dev
FIRE: what is the transactional outbox pattern
FIRE: why do I need saveAndFlush to get a creation timestamp
FIRE: Testcontainers HTTP 400 from the Docker Desktop CLI socket
FIRE: what does the Storybook decorator pattern do for Redux providers
FIRE: why did IntelliJ lose src/test/java as a test sources root
FIRE: how does the saga pattern work with choreography

QUIET: Spring Data JPA pagination with sorting on multiple columns
QUIET: Testcontainers reuse across test classes in Spring Boot
QUIET: Keycloak client scopes and custom claim mappers
QUIET: Storybook accessibility addon for component checks
QUIET: Liquibase rollback strategy for a failed migration
QUIET: Kafka consumer group rebalancing in an event driven system
QUIET: Maven multi module build with a shared parent pom
QUIET: React hook for detecting clicks outside a dropdown
QUIET: Docker Compose healthcheck for a Postgres service
QUIET: Binding a nested YAML list into a Spring Boot configuration record
QUIET: how do I rename a file in bash
QUIET: what is the time complexity of quicksort
QUIET: write a regex that matches an email address
QUIET: how do I set up a Kubernetes ingress with cert-manager
QUIET: what is the difference between a mutex and a semaphore

# --- Widened 2026-08-27, B-037's wide-sweep plan (docs/TODO.md) ---
#
# Same rule as above and as neighbour-labels.txt/query-ecosystems.txt: written from the
# corpus's note titles/topics before any score was measured on this batch, targeting
# ecosystems the original 25 under-represent (Keycloak+JWT, Liquibase, DDD/hexagonal/CQRS,
# MapStruct, Spring Security beyond @Value, Docker init-container sequencing, Spring Boot 4
# breaking changes, Storybook+Next.js, Continue.dev config precedence). None of these ten
# FIRE prompts were chosen because of an expected score — each names an existing note this
# corpus already answers, the same test the original ten pass.
#
# The new QUIET prompts split the same way as the original 15: ten adjacent-topic hard
# negatives (a related note exists in the widened ecosystems above, but does not answer
# this specific sub-question) and five off-corpus prompts in the same general-programming
# register as the original five, on topics that appear nowhere in this vault.

FIRE: what does @PreAuthorize do for Spring Security method-level access control
FIRE: why is MapStruct warning about unmapped target properties
FIRE: what is CQRS and how does it relate to event-driven messaging
FIRE: what's the difference between hexagonal architecture ports and adapters
FIRE: how do I set up Keycloak Google social login with Nimbus JWT
FIRE: why does docker-compose need health-gated init containers for sequencing
FIRE: why did Spring Boot Initializr generate spring-boot-starter-webmvc instead of spring-boot-starter-web
FIRE: what breaking changes does Spring Boot 4 introduce for artifact renames and test modules
FIRE: why does Storybook with Next.js App Router need appDirectory true
FIRE: why does Continue.dev's config.ts silently override config.json

QUIET: Spring Security custom AccessDecisionVoter for role hierarchies
QUIET: MapStruct custom converter for nested DTO mapping
QUIET: Keycloak custom login theme customization
QUIET: Liquibase changelog authoring best practices for large teams
QUIET: Docker multi-stage build caching strategies
QUIET: DDD bounded context mapping between two microservices
QUIET: event sourcing snapshotting strategy for CQRS aggregates
QUIET: Continue.dev remote model provider authentication setup
QUIET: RAG chunking strategy for long documents
QUIET: Spring Boot Actuator health check endpoint customization
QUIET: how do I reverse a linked list in place
QUIET: what is the CAP theorem in distributed systems
QUIET: how do I configure nginx as a reverse proxy
QUIET: what's the difference between TCP and UDP
QUIET: how do I center a div with CSS flexbox
