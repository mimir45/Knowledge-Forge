{
  "_notes": [
    "Knowledge Forge — Claude Code hook bindings (DESIGN §13, ADDENDUM §C).",
    "Phase 6 closed the packaging gap this note used to describe: this file is now",
    "auto-loaded by .claude-plugin/plugin.json's default hooks/hooks.json discovery",
    "once the plugin is installed via the marketplace — no manual copy into",
    "settings.json needed.",
    "",
    "Resume gotcha: SessionStart re-runs on --continue/--resume (with source: resume) —",
    "that's expected, forge session-context's output is idempotent and cheap to re-print.",
    "But every OTHER hook's output on a resumed session is REPLAYED from the saved",
    "transcript text, not re-executed. So nothing time-sensitive belongs in",
    "UserPromptSubmit (or any hook here) — a stale recall hit from the original session is",
    "expected on resume, not a bug in forge intent.",
    "",
    "Command paths below use ${CLAUDE_PLUGIN_ROOT}, substituted by Claude Code to the",
    "plugin's installed directory — they no longer hardcode /Users/mimir45/TIL."
  ],

  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}\"/hooks/session-context",
            "timeout": 5
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}\"/hooks/user-prompt-intent",
            "timeout": 2
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}\"/hooks/session-end-capture",
            "timeout": 10
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "WebFetch",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}\"/hooks/post-tool-cache-source",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
